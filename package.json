{
  "name": "list_belanja_app",
  "version": "1.0.0",
  "description": "belajar membuat list belanja",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "serve -S static",
    "start-be": "json-server-auth db.json --port 8000 --watch --host 0.0.0.0"
  },
  "author": "fadliselaz",
  "license": "MIT",
  "dependencies": {
    "json-web-token": "^3.2.0",
    "serve": "^14.0.1"
  },
  "devDependencies": {
    "json-server": "^0.17.0",
    "json-server-auth": "^2.1.0"
  }
}
